// Initial logic for calculating tip using ternary operator
let bill = 275; // Example bill amount
let tip = (bill >= 50 && bill <= 300) ? bill * 0.15 : bill * 0.20;
console.log(`Initial logic for calculating tip using ternary operator.`); // Commit message
